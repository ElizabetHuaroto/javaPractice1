package com.upc;
//una interface es un contrato que dice que cualquier clase implementada debe cumplir con estas funciones
public interface IFundation {
    boolean putDonation(Integer entero);
}
